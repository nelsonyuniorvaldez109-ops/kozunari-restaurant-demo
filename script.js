const header=document.querySelector('.site-header'),toggle=document.querySelector('.menu-toggle'),nav=document.querySelector('nav');
toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',!open);nav.classList.toggle('open',!open);document.body.classList.toggle('menu-open',!open)});
nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{toggle.setAttribute('aria-expanded','false');nav.classList.remove('open');document.body.classList.remove('menu-open')}));
addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>80),{passive:true});
const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;
if(!reduce){const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('shown')}),{threshold:.16});document.querySelectorAll('.section-head,.philosophy-copy,.chef-copy,.gallery-title,.reserve-title,.menu-list').forEach(el=>observer.observe(el));const moving=document.querySelectorAll('.parallax');addEventListener('scroll',()=>{moving.forEach(el=>{const r=el.getBoundingClientRect(),v=(innerHeight-r.top)/(innerHeight+r.height);el.style.setProperty('--shift',`${(v-.5)*26}px`)})},{passive:true})}
const form=document.querySelector('#reservation-form'),message=document.querySelector('.form-message');form.addEventListener('submit',e=>{e.preventDefault();if(!form.checkValidity()){message.textContent='Please complete each field so we can prepare for your visit.';form.reportValidity();return}const guestName=new FormData(form).get('name');message.textContent=`Thank you, ${guestName}. This is a portfolio demo—your table request has not been transmitted.`;form.reset()});
